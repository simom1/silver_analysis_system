# 🛡️ GitHub安全上传指南

## 🚨 常见敏感信息类型

### 1. 认证信息
- ❌ GitHub Personal Access Token (PAT)
- ❌ API密钥 (OpenAI, 各种服务商)
- ❌ 数据库密码
- ❌ SSH私钥
- ❌ 服务器密码

### 2. 个人信息
- ❌ 真实邮箱地址
- ❌ 电话号码
- ❌ 身份证号码
- ❌ 银行卡信息

### 3. 系统信息
- ❌ 内网IP地址
- ❌ 服务器配置
- ❌ MT5真实账户信息
- ❌ 交易服务器地址

## 🔍 上传前检查清单

### ✅ 必做检查
1. **运行敏感信息检查**
   ```bash
   # 双击运行
   敏感信息检查.bat
   ```

2. **检查.gitignore文件**
   - 确保敏感文件类型已添加
   - 验证规则是否生效

3. **手动代码审查**
   - 搜索关键词：password, token, key, secret
   - 检查配置文件
   - 验证示例数据是否为虚拟数据

### 📋 具体检查步骤

#### 步骤1: 自动扫描
```bash
# 运行自动检查工具
敏感信息检查.bat
```

#### 步骤2: 手动检查
```bash
# 搜索常见敏感信息模式
findstr /r "password\|token\|key\|secret" *.py *.json *.bat *.md
```

#### 步骤3: Git状态检查
```bash
# 查看将要提交的文件
git status
git diff --cached
```

## 🛠️ 敏感信息处理方法

### 方法1: 环境变量
```python
import os

# ❌ 错误做法
api_key = "sk-1234567890abcdef"

# ✅ 正确做法
api_key = os.getenv('OPENAI_API_KEY')
```

### 方法2: 配置文件模板
```python
# config_template.json
{
    "api_key": "YOUR_API_KEY_HERE",
    "database_url": "YOUR_DATABASE_URL_HERE"
}

# 实际使用 config.json (添加到.gitignore)
{
    "api_key": "real_api_key_value",
    "database_url": "real_database_url"
}
```

### 方法3: 占位符替换
```python
# ❌ 真实信息
MT5_LOGIN = 12345678
MT5_PASSWORD = "real_password"

# ✅ 占位符
MT5_LOGIN = "YOUR_MT5_LOGIN"
MT5_PASSWORD = "YOUR_MT5_PASSWORD"
```

## 🚨 紧急处理：已上传敏感信息

### 如果已经推送敏感信息到GitHub：

#### 1. 立即撤销敏感信息
- 更改密码/令牌
- 撤销API密钥
- 更新访问权限

#### 2. 清理Git历史
```bash
# 从历史中完全删除文件
git filter-branch --force --index-filter \
"git rm --cached --ignore-unmatch sensitive_file.txt" \
--prune-empty --tag-name-filter cat -- --all

# 强制推送
git push origin --force --all
```

#### 3. 联系GitHub支持
如果是高度敏感信息，可以联系GitHub支持团队协助处理。

## 📚 最佳实践

### 1. 开发环境配置
```bash
# 创建 .env 文件 (添加到.gitignore)
OPENAI_API_KEY=your_real_key
MT5_LOGIN=your_real_login
MT5_PASSWORD=your_real_password
```

### 2. 代码中使用
```python
from dotenv import load_dotenv
import os

load_dotenv()

api_key = os.getenv('OPENAI_API_KEY')
```

### 3. 文档说明
```markdown
## 配置说明
1. 复制 `.env.example` 为 `.env`
2. 填入你的真实配置信息
3. `.env` 文件不会被上传到GitHub
```

## 🔧 工具推荐

### 1. 预提交钩子
```bash
# 安装 pre-commit
pip install pre-commit

# 配置 .pre-commit-config.yaml
repos:
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.4.0
    hooks:
      - id: detect-secrets
```

### 2. GitHub Secret Scanning
- GitHub会自动扫描推送的代码
- 发现敏感信息会阻止推送
- 提供修复建议

### 3. 第三方工具
- **GitLeaks**: 检测Git仓库中的敏感信息
- **TruffleHog**: 搜索Git历史中的敏感数据
- **detect-secrets**: Yelp开源的敏感信息检测工具

## ⚡ 快速检查命令

```bash
# 检查当前目录所有文件
grep -r "password\|token\|key\|secret" . --exclude-dir=.git

# 检查特定文件类型
find . -name "*.py" -exec grep -l "api_key\|password" {} \;

# 检查Git暂存区
git diff --cached | grep -i "password\|token\|key"
```

## 📞 应急联系

如果发现已经泄露敏感信息：
1. 立即更改相关密码/密钥
2. 运行 `清理敏感信息.bat`
3. 检查GitHub Security页面
4. 必要时联系相关服务提供商

---

**记住：预防胜于治疗！每次上传前都要进行安全检查。** 🛡️